# GE1 Closed-Form Readout, Job 3351524

## Supplied execution evidence

The designated reviewer supplied the complete terminal transcript and the
finalized `readout_results.json` for read-only audit. The JSON available at
review time has SHA-256
`d86aa2d50b017c38456e8dd4e32f0863b0b1e8ffea86924346be23edafc04dab`.

| Item | Evidence value |
|---|---|
| Job | `3351524` |
| Exact commit | `4dff7e0ed1fea7076b64e2513689088aff4d8078` |
| Protocol | `GE1-C7-CLOSED-FORM-READOUT-v2` |
| Result schema | `GE1-C7-CLOSED-FORM-READOUT-RESULTS-v2` |
| State / exit | `COMPLETED` / `0:0` |
| Elapsed / batch MaxRSS | `00:08:55` / `606940K` |
| Runtime | Python `3.8.13`, PyTorch `1.11.0`, CPU only, one thread |
| Pipelines | 24 executed, 8 structured nonexecuted |
| Primary hypotheses | 16 unique |
| Permutations | 999 |

The transcript records successful validation and checksum verification of the
complete five-file artifact. Only the final result JSON and terminal transcript
were supplied locally for this repository update; the artifact manifest,
`SHA256SUMS`, resolved-input record, and other artifact files are not committed
here. Therefore the JSON's local hash cannot be independently tied to the
five-file artifact manifest from the files currently available.

The JSON declares the following exact preserved-input hashes:

- detached features:
  `87dab4841f64f0c1b27db6676852da7cd980ba1bde61e81809fde3af745cc01c`;
- feature manifest:
  `29220bda0677636627069ce6565bf367065f1ceaa3c2d86d3174058838598450`;
- labels:
  `70f82727ebe35613bcbae1267924d9a327d9744a757b6d076c8028e261cfe71f`.

## Scientific disposition and boundary

For each flat/typed-graph by extrude/revolve row, the prospective result fields
are false for controlled C accessibility, controlled upstream accessibility,
decoder-state material improvement over the common scalar path, and a decoder
conditioning candidate. The result therefore did not establish controlled
magnitude-class accessibility in any tested arm/type combination. This is a
null/inconclusive accessibility result under the tested features and frozen
rules, not proof of information absence or capacity exhaustion.

The record does not complete or reinterpret ADR-0011 or the earlier
representation screen, identify an exact defect, predict 48/48, establish
encoder superiority, authorize a categorical head or other repair, authorize
Stage 6 or C8, or open protected access. ADR-0013's later reviewer decision is
a separate prospective authorization, not an authorization emitted by job
3351524.
